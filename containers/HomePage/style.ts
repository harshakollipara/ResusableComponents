import { StyleSheet, Platform } from 'react-native';
import { baseTheme } from '../../styles/baseTheme';


export const style = StyleSheet.create({
    twoFieldsRow: {
        flexDirection: 'row',
    },
    formSpacer: { flexGrow: 1, flexBasis: 0 },
});


